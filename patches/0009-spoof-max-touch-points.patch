# cosmium patch: 0009-spoof-max-touch-points
# Authored: 2026-05-17
# Chromium base: see ../VERSION
#
# Describe the detection vector this patch fixes:
# - target file(s): third_party/blink/renderer/core/frame/navigator.cc
# - what changed: Navigator::maxTouchPoints consults --cosmium-max-touch-points
# - profile fields read: hardware.max_touch_points
# - test: navigator.maxTouchPoints; convertibles + mobile devices must report >= 1
#         (https://browserleaks.com/javascript)
diff --git a/third_party/blink/renderer/core/frame/navigator.cc b/third_party/blink/renderer/core/frame/navigator.cc
--- a/third_party/blink/renderer/core/frame/navigator.cc
+++ b/third_party/blink/renderer/core/frame/navigator.cc
@@ -1,5 +1,7 @@
 #include "third_party/blink/renderer/core/frame/navigator.h"

+#include "base/command_line.h"
+#include "base/strings/string_number_conversions.h"
 #include "third_party/blink/public/common/features.h"
 #include "third_party/blink/renderer/core/frame/local_frame.h"

@@ -120,6 +122,14 @@ String Navigator::GetAcceptLanguages() {
 }

 int Navigator::maxTouchPoints() const {
+  const auto* cosmium_cmd = base::CommandLine::ForCurrentProcess();
+  if (cosmium_cmd->HasSwitch("cosmium-max-touch-points")) {
+    int cosmium_value = 0;
+    if (base::StringToInt(
+            cosmium_cmd->GetSwitchValueASCII("cosmium-max-touch-points"),
+            &cosmium_value)) {
+      return cosmium_value;
+    }
+  }
   if (!GetFrame())
     return 0;
   return GetFrame()->GetSettings()->GetMaxTouchPoints();
