diff --git a/content/browser/renderer_host/render_process_host_impl.cc b/content/browser/renderer_host/render_process_host_impl.cc
index 2bc557c1d9..2cd0ee5861 100644
--- a/content/browser/renderer_host/render_process_host_impl.cc
+++ b/content/browser/renderer_host/render_process_host_impl.cc
@@ -3345,6 +3345,22 @@ void RenderProcessHostImpl::PropagateBrowserCommandLineToRenderer(
   // Propagate the following switches to the renderer command line (along
   // with any associated values) if present in the browser command line.
   static const char* const kSwitchNames[] = {
+      // cosmium: every spoofing switch below is consumed inside Blink, i.e. in
+      // this renderer, via base::CommandLine::ForCurrentProcess(). Without
+      // being listed here the renderer's command line never carries them,
+      // HasSwitch() is false, and every override silently returns the real
+      // value while the browser process accepts the flag without complaint.
+      "cosmium-platform",
+      "cosmium-languages",
+      "cosmium-hardware-concurrency",
+      "cosmium-device-memory",
+      "cosmium-ua-platform",
+      "cosmium-max-touch-points",
+      "cosmium-color-depth",
+      "cosmium-webgl-vendor",
+      "cosmium-webgl-renderer",
+      "cosmium-audio-base-latency",
+      "cosmium-audio-output-latency",
       switches::kDisableInProcessStackTraces,
       sandbox::policy::switches::kDisableSeccompFilterSandbox,
       sandbox::policy::switches::kNoSandbox,
